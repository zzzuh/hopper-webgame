import { Configuration } from "./Configuration";

class Solver {
    
    constructor() {};

    public solve(config: Configuration): Configuration[] {
        let predecessors = new Map<Configuration, Configuration>();
        let queue: Configuration[];
        
    }
}